% maximum number of steps 
#const maxStepsConst = 10.

% steps declaration
step(0..maxStepsConst).

% board quality
quality(1..2).

% number
number(10000).
number(20000).
number(30000).
number(40000).
number(50000).

% payment type
paymentType(board;shipping).
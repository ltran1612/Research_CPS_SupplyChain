{
  "scripts": {
    "start": "node server.js",
    "format": "npx prettier . --write",
    "test": "npx prettier . --check"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "formidable": "^2.1.1",
    "tmp": "^0.2.1"
  },
  "devDependencies": {
    "prettier": "3.0.2"
  }
}
